---
import NavMenu from '../components/NavMenu.astro';
import Footer from '../components/Footer.astro';

export interface Props {
  title: string;
  description?: string;
  currentFamily?: string;
  creditType?: 'none' | 'smplr' | 'verovio' | 'both';
  includeWidgetsCss?: boolean;
}

const {
  title,
  description = "Bezpłatne, interaktywne ćwiczenia z kształcenia słuchu: interwały, trójdźwięki, gamy, puzzle, dyktanda i rytm.",
  currentFamily,
  creditType = 'smplr',
  includeWidgetsCss = false,
} = Astro.props;
---
<!doctype html>
<html lang="pl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,500;0,9..144,600;0,9..144,700;1,9..144,400&family=Plus+Jakarta+Sans:wght@400;500;600;700&family=Work+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/css/site.css">
  {includeWidgetsCss && <link rel="stylesheet" href="/assets/css/widgets.css">}
</head>
<body>

<header class="site-header">
  <div class="container site-header-inner">
    <a class="brand" href="/">
      <span class="brand-title">ksza.pl<span class="brand-dot" aria-hidden="true"></span></span>
      <small>kształcenie słuchu</small>
    </a>
    <button class="nav-toggle" aria-controls="site-menu" aria-expanded="false">Menu</button>
    <NavMenu currentFamily={currentFamily} />
  </div>
</header>

<slot />

<Footer creditType={creditType} />

<slot name="scripts" />
</body>
</html>
